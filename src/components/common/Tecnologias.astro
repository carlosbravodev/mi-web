---
import { Image } from "astro:assets";
const { url, titulo } = Astro.props;
---

<div
  class="flex flex-col justify-center items-center p-4 gap-2 border border-zinc-800 rounded"
>
  <Image src={url} alt={titulo} class="h-10 lg:h-16" />
  <h4 class="font-sans">{titulo}</h4>
</div>
